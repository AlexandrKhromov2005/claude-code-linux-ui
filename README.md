# claude-code-linux-ui

Terminal UI for Claude on Linux. Chat and agent modes on top of the Claude Code
CLI, with project-scoped history and an approval gate for file edits and commands.

Status: in development.

## Build

    go build ./...

## Run

    ./claude-code-linux-ui

Requires the `claude` CLI installed and authenticated.
